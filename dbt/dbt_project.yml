name: european_energy
version: "1.0.0"
config-version: 2

profile: european_energy

model-paths:
  - models

clean-targets:
  - target
  - dbt_packages

models:
  european_energy:
    staging:
      +materialized: view
      +schema: staging

    intermediate:
      +materialized: view
      +schema: intermediate

    marts:
      +materialized: table
      +schema: marts
